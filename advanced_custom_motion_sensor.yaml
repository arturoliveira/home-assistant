condition:
  - condition: template
    value_template: >
      {% if automation_blocker_entities | list | length == 0 %}
        true
      {% else %}
        {% for entity in automation_blocker_entities %}
          {% if states(entity) == (automation_blocker_boolean | string) %}
            false
          {% endif %}
        {% endfor %}
        true
      {% endif %}